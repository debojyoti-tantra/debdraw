import React from 'react'
import { useParams } from 'next/navigation'

const page = () => {
    // const {userId} = useParams()

    return (
        <div>
            userId
        </div>
    )
}

export default page